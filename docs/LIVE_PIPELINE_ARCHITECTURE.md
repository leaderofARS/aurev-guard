# LIVE Pipeline Integration - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     FRONTEND (Port 3000)                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Dashboard / Wallet Analysis Page                        │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ LivePipelineProcessor Component                     │ │   │
│  │  │ ┌─────────────────────────────────────────────────┐ │ │   │
│  │  │ │ User enters wallet address                      │ │ │   │
│  │  │ │ Clicks "Start Analysis"                         │ │ │   │
│  │  │ │ ↓                                                │ │ │   │
│  │  │ │ Progress bar visualization (0-100%)             │ │ │   │
│  │  │ │ Real-time status updates                        │ │ │   │
│  │  │ │ ↓                                                │ │ │   │
│  │  │ │ Results display:                                │ │ │   │
│  │  │ │ - Risk Score                                    │ │ │   │
│  │  │ │ - Anomaly Score                                 │ │ │   │
│  │  │ │ - Extracted Features (18 dimensions)            │ │ │   │
│  │  │ │ - Transaction Metadata                          │ │ │   │
│  │  │ └─────────────────────────────────────────────────┘ │ │   │
│  │  └──────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────┬──────────────────────────────────────────┘
                         │ HTTP/REST
                         │ POST /api/live-pipeline/start
                         │ GET  /api/live-pipeline/status/:jobId
                         │ GET  /api/live-pipeline/results/:address
                         │
┌────────────────────────▼──────────────────────────────────────────┐
│                    BACKEND (Port 5000)                            │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │              Live Pipeline Routes                           │  │
│  │  /api/live-pipeline/start ────────┐                        │  │
│  │  /api/live-pipeline/status/:jobId │                        │  │
│  │  /api/live-pipeline/results/:addr │                        │  │
│  └────────────────────────────────────┼────────────────────────┘  │
│                                       │                            │
│  ┌────────────────────────────────────▼────────────────────────┐  │
│  │         Live Pipeline Controller                            │  │
│  │  ┌────────────────────────────────────────────────────────┐ │  │
│  │  │ startPipeline()                                        │ │  │
│  │  │ ↓                                                       │ │  │
│  │  │ - Generate jobId                                       │ │  │
│  │  │ - Create PipelineJob instance                          │ │  │
│  │  │ - Store in jobsMap                                     │ │  │
│  │  │ - Simulate async processing                           │ │  │
│  │  │ - Update progress (0→100%)                            │ │  │
│  │  │ - Call Orchestrator with features                     │ │  │
│  │  │ - Return results                                       │ │  │
│  │  └────────────────────────────────────────────────────────┘ │  │
│  │                                                              │  │
│  │  ┌────────────────────────────────────────────────────────┐ │  │
│  │  │ getPipelineStatus()                                    │ │  │
│  │  │ ↓                                                       │ │  │
│  │  │ - Lookup jobId in jobsMap                              │ │  │
│  │  │ - Return current status & progress                     │ │  │
│  │  └────────────────────────────────────────────────────────┘ │  │
│  │                                                              │  │
│  │  ┌────────────────────────────────────────────────────────┐ │  │
│  │  │ getPipelineResults()                                   │ │  │
│  │  │ ↓                                                       │ │  │
│  │  │ - Filter jobs by walletAddress                         │ │  │
│  │  │ - Sort by date (newest first)                          │ │  │
│  │  │ - Return last 50 results                               │ │  │
│  │  └────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │              Data Models (Pipeline.js)                        │  │
│  │  - PipelineJob                                               │  │
│  │  - PipelineResult                                            │  │
│  │  - PipelineFeatures (18 dimensions)                          │  │
│  │  - PipelineConfig                                            │  │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │            Middleware (walletAuth.js)                        │  │
│  │  - checkPayment()        → Verify 2 ADA payment              │  │
│  │  - checkWalletWhitelist() → Optional wallet filtering        │  │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │         Configuration (config/index.js)                      │  │
│  │  - BLOCKFROST_API_KEY                                        │  │
│  │  - ORCHESTRATOR_URL                                          │  │
│  │  - CARDANO_NETWORK                                           │  │
│  │  - LIVE_PIPELINE_TIMEOUT                                     │  │
│  │  - PAYMENT_REQUIRED, PAYMENT_ADDRESS, PAYMENT_AMOUNT_ADA     │  │
│  └────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                         │
      ┌──────────────────┼───────────────────┐
      │                  │                   │
      │ Features         │ Status            │ Results
      │ (18 dims)        │ (progress)        │ (predictions)
      │                  │                   │
┌─────▼────────┐  ┌──────▼──────┐  ┌────────▼────────┐
│   Blockchain │  │  Job Status │  │  Orchestrator   │
│     (Mock)   │  │  Tracking   │  │  (Port 8080)    │
│              │  │             │  │                 │
│ - Blockfrost │  │ - jobsMap   │  │  AI Models:     │
│ - Ready for  │  │ - progress  │  │  - Random Forest│
│   real impl  │  │ - timestamp │  │  - Isolation    │
│              │  │             │  │    Forest       │
└──────────────┘  └─────────────┘  └─────────────────┘
```

## Data Flow Sequence

```
┌────────────────────────────────────────────────────────────────┐
│                    DATA FLOW SEQUENCE                          │
└────────────────────────────────────────────────────────────────┘

1. USER INITIATES ANALYSIS
   ┌─────────────┐
   │   Frontend  │ User clicks "Start Analysis"
   └──────┬──────┘
          │
          │ POST /api/live-pipeline/start
          │ { walletAddress, transactionId }
          │
          ▼
   ┌──────────────────┐
   │  Backend Server  │
   └──────┬───────────┘
          │
          ├─ Generate unique jobId
          ├─ Create PipelineJob
          ├─ Store in jobsMap
          └─ Return { jobId, status: 'started' }
          │
          ▼


2. STATUS POLLING (Frontend polls every 2 seconds)
   ┌─────────────┐
   │   Frontend  │ GET /api/live-pipeline/status/jobId
   └──────┬──────┘
          │
          ▼
   ┌──────────────────────────────────┐
   │  Backend - getPipelineStatus()   │
   │  - Lookup jobId in jobsMap       │
   │  - Return { status, progress }   │
   └──────┬───────────────────────────┘
          │
          ▼
   ┌─────────────┐
   │   Frontend  │ Update progress bar
   └─────────────┘
          │
          ▼
   (continues every 2 seconds until complete)


3. BACKGROUND PROCESSING (Server)
   ┌──────────────────────────────────┐
   │ simulate async processing()       │
   │                                   │
   │ Time 0s:   progress = 10%         │
   │ Time 1s:   progress = 30%         │
   │ Time 2s:   progress = 50%         │
   │ Time 3s:   progress = 70%         │
   │ Time 4s:   progress = 90%         │
   │ Time 5s:   Orchestrator call      │
   │ Time 6s:   results received       │
   │ Time 7s:   progress = 100%        │
   │            status = 'completed'   │
   │                                   │
   │ Orchestrator Integration:         │
   │ - Extract 18 features             │
   │ - POST to orchestrator/predict    │
   │ - Receive risk_score              │
   │ - Receive anomaly_score           │
   │ - Return full prediction          │
   └──────┬───────────────────────────┘
          │
          ▼


4. RESULTS RETRIEVAL
   ┌─────────────┐
   │   Frontend  │ Detects status === 'completed'
   └──────┬──────┘
          │
          │ Optional: GET /api/live-pipeline/results/:address
          │
          ▼
   ┌──────────────────────────────────┐
   │  Backend - getPipelineResults()  │
   │  - Filter completed jobs         │
   │  - Sort by date                  │
   │  - Return last 50 results        │
   └──────┬───────────────────────────┘
          │
          ▼
   ┌─────────────┐
   │   Frontend  │ Display results:
   │             │ - Risk Score
   │             │ - Anomaly Score
   │             │ - Features
   │             │ - Metadata
   └─────────────┘
```

## Feature Extraction Pipeline

```
┌─────────────────────────────────────────────────────────────┐
│              FEATURE EXTRACTION (18 Dimensions)             │
└─────────────────────────────────────────────────────────────┘

Blockchain Data (Cardano Testnet)
          │
          ▼
    ┌──────────────────────────────────────┐
    │   Extract Transaction Data           │
    │ - Transaction count                  │
    │ - Amount flows                       │
    │ - Address interactions               │
    │ - Timing patterns                    │
    │ - Smart contract flags               │
    └──────┬───────────────────────────────┘
           │
           ▼
    ┌──────────────────────────────────────────────┐
    │  Compute 18 Features                         │
    │                                              │
    │  Flow Features (3):                          │
    │  1. tx_count_24h                             │
    │  2. total_received                           │
    │  3. total_sent                               │
    │                                              │
    │  Size Features (2):                          │
    │  4. max_tx_size                              │
    │  5. avg_tx_size                              │
    │                                              │
    │  Balance Features (2):                       │
    │  6. net_balance_change                       │
    │  7. collateral_ratio                         │
    │                                              │
    │  Address Features (2):                       │
    │  8. unique_counterparties                    │
    │  9. counterparty_diversity                   │
    │                                              │
    │  Velocity Features (2):                      │
    │  10. tx_per_day                              │
    │  11. velocity_hours                          │
    │                                              │
    │  Time Features (2):                          │
    │  12. active_days                             │
    │  13. timing_entropy                          │
    │                                              │
    │  Pattern Features (4):                       │
    │  14. burstiness                              │
    │  15. smart_contract_flag                     │
    │  16. high_value_ratio                        │
    │  17. inflow_outflow_asymmetry                │
    │  18. [reserved for future use]               │
    │                                              │
    └──────┬───────────────────────────────────────┘
           │
           ▼
    ┌──────────────────────────────────┐
    │  Feature Vector (18 dimensions)  │
    │  [f1, f2, f3, ..., f18]          │
    └──────┬───────────────────────────┘
           │
           │ POST to Orchestrator
           │
           ▼
    ┌──────────────────────────────────┐
    │  Orchestrator AI Models          │
    │                                  │
    │  Model 1: Random Forest          │
    │  Model 2: Isolation Forest       │
    │                                  │
    └──────┬───────────────────────────┘
           │
           ▼
    ┌──────────────────────────────────┐
    │  Predictions                     │
    │ - risk_score (0.0-1.0)           │
    │ - anomaly_score (0.0-1.0)        │
    │ - prediction (HIGH_RISK/LOW_RISK)│
    │ - shap_values (feature importance)
    └──────────────────────────────────┘
```

## Component Integration Map

```
┌─────────────────────────────────────────────────────────────┐
│                    FILES & DEPENDENCIES                     │
└─────────────────────────────────────────────────────────────┘

Frontend Layer:
┌─────────────────────────────────┐
│ LivePipelineProcessor.jsx        │ (234 lines)
│ ├─ Uses: axios (HTTP)           │
│ ├─ Uses: lucide-react (icons)   │
│ ├─ Props: walletAddress         │
│ └─ Props: onComplete callback   │
└──────────────┬──────────────────┘
               │ HTTP Calls
               │

Backend Layer:
┌─────────────────────────────────────────────────────────────┐
│ server.js (imports all routes)                              │
├─ Imports: livePipeline.js                                  │
└─────────────────────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│ routes/livePipeline.js (15 lines)                           │
├─ POST /start → startPipeline controller                     │
├─ GET /status/:jobId → getPipelineStatus controller         │
├─ GET /results/:address → getPipelineResults controller     │
└─────────────────────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│ controllers/livePipelineController.js (223 lines)           │
├─ startPipeline()                                            │
│  └─ Creates PipelineJob                                    │
│  └─ Calls startPythonPipeline()                            │
├─ getPipelineStatus()                                        │
│  └─ Queries jobsMap                                        │
├─ getPipelineResults()                                       │
│  └─ Filters and sorts results                              │
└─────────────────────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│ models/Pipeline.js (152 lines)                              │
├─ PipelineJob class                                          │
├─ PipelineResult class                                       │
├─ PipelineFeatures class (18 dimensions)                     │
├─ PipelineConfig class                                       │
└─────────────────────────────────────────────────────────────┘
               │
               ├──────────────────┬──────────────────┐
               │                  │                  │
               ▼                  ▼                  ▼
    ┌────────────────┐  ┌────────────────┐  ┌──────────────┐
    │ config/index.js│  │ middleware/    │  │ store/       │
    │                │  │ walletAuth.js  │  │ (optional)   │
    │ - BLOCKFROST   │  │                │  │              │
    │ - ORCHESTRATOR │  │ checkPayment() │  │ Results DB   │
    │ - NETWORK      │  │ whitelist()    │  │ (future)     │
    │ - TIMEOUTS     │  │                │  │              │
    └────────────────┘  └────────────────┘  └──────────────┘
```

---

**Legend:**
- 📊 Data Flow
- 🔄 Processing
- 📤 Output
- ⚙️ Configuration
